# Write a program that:

# Takes a number as input from the user (float).
# Takes the number of decimal places to round to (integer).
# Outputs the rounded number.

# Write code here
number = float(input())
rnumber = int(input())

print(round(number,rnumber))
